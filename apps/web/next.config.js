/**
 * Next.js config: enable static HTML export for GitHub Pages and CI static serve.
 * Setting `output: 'export'` makes Next build produce a static export.
 */
const nextConfig = {
  output: 'export',
};

export default nextConfig;
